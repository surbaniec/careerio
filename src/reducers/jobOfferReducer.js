import {
  GET_JOB_OFFER,
  GET_JOB_OFFERS,
  ADD_JOB_OFFER,
  UPDATE_JOB_OFFER,
  DELETE_JOB_OFFER,
} from '../actions/types';

const jobOffer = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default jobOffer;
