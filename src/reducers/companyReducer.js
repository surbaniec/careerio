import {
  GET_COMPANY,
  GET_COMPANIES,
  ADD_COMPANY,
  UPDATE_COMPANY,
  DELETE_COMPANY,
} from '../actions/types';

const company = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default company;
